%tr
  %td= item['name']
  %td= item['unit']
  %td= item['quantity']
  %td= number_to_currency(item['price'], unit: '$')
  %td= number_to_currency(item['price'].to_f * item['quantity'].to_f, unit: '$')
