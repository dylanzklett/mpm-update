%table.table.table-striped.table-bordered.items-table{align: :center}
  %tr
    %th.number-column{rowspan: 2} #
    %th{rowspan: 2} Num curtains
    %th{rowspan: 2} Width per curtain
    %th{rowspan: 2} Finished Length
    %th{rowspan: 2} Pattern/color
    %th{colspan: 3} Mark drapery with the following
  %tr
    %th Room
    %th Width
    %th Length
  - items.each_with_index do |item, index|
    %tr
      %td= index + 1
      %td= item.quantity
      %td= item.width_per_curt
      %td= item.convert(:finished_length, 'in')
      %td= item.pattern_color
      %td= item.room
      %td= item.convert(:width, 'in')
      %td= item.convert(:height, 'in')
